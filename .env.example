# انسخ هذا الملف إلى .env وعدّل القيم
POSTGRES_DB=voucherdb
POSTGRES_USER=voucheruser
POSTGRES_PASSWORD=voucherpass

JWT_SECRET=change_this_to_a_long_random_string

# البورت الخارجي للتطبيق (80 للإنتاج)
APP_PORT=80
